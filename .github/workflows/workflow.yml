name: workflow
on: push

jobs:
    felles-workflow:
        uses: navikt/flex-github-actions-workflows/.github/workflows/next-js.yml@master
        secrets: inherit
        with:
            static-files-path: sykepengesoknad
            base-path: syk/sykepengesoknad
