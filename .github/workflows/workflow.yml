name: workflow
on:
  workflow_dispatch:
  push:

jobs:
  felles-workflow:
    uses: navikt/flex-github-actions-workflows/.github/workflows/next-js.yml@parallell-gar-cypress
    secrets: inherit
    with:
      app: sykepengesoknad
      base-path: syk/sykepengesoknad
      sentry-project: sykepengesoknad
      sentry-enabled: false
